@using EstudoMVC.ViewModels;
@model LoginViewModel
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Login";
}
<h1>Login</h1>

<div class="row">
    <div class="col-md6 offset-3">
        <p>
            <h4>Login Account</h4>
        </p>
        @if (TempData["Error"] != null){
            <div class="col-md-12 alert alert-danger">
                <span><b>Sorry!</b> - @TempData["Error"]</span>

            </div>
        }

        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Login">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form=group">
                        <label asp-for="EmailAddres" class="control-label">Email</label>
                        <input asp-for="EmailAddres" class="form-control" placeholder="Email" />
                        <span asp-validation-for="EmailAddres" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Password">Senha</label>
                        <input asp-for="Password" class="form-control" placeholder="Senha" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input class="btn btn-outline-success form-floating" type="submit" value="Log in" />
                        <a class="btn btn-outline-secondary" asp-controller="TouristAttraction" asp-action="Index">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

